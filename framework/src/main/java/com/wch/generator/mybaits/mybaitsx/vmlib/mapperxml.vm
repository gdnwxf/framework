<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dfire.retail.mapper.user.PerformanceMapper">
    <resultMap id="BaseResultMap" type="com.dfire.retail.bo.user.Performance">
		#foreach( $c in  $primaryKeyColumns)
		<id column="${c.actualColumnName}" property="${c.javaProperty}" jdbcType="${c.jdbcTypeName}" />
		#end
		#foreach( $c in  $baseColumns)
		<result column="${c.actualColumnName}" property="${c.javaProperty}" jdbcType="${c.jdbcTypeName}"  />
		#end
    </resultMap>
    <sql id="Base_Column_List" >
   		 #set($idx = 0)
   		 #set($len = $!{allColumns.size()} - 1 )
   		 #set($cycle = $len  / 3 )
   		 #set($nums=[0 .. $cycle])
   		 #set($c = 0 )
	    #foreach( $idx in $nums )
	    	#set($c0 = $c)
	    	#set($c1 = $c + 1 )
	    	#set($c2 = $c + 2 )
	    	 #if(  $idx <  $cycle    )  
   		         ${allColumns[$c0].actualColumnName}, ${allColumns[$c1].actualColumnName},  ${allColumns[$c2].actualColumnName}, 
	    	 #end
	    	 #set($c = ($!idx + 1 ) * 3 )
	    	 #set($idx = $!idx + 1)
	    #end
	    
	          #set($a = $len - $c ) 
	        #set($c0 = $c)
	        #set($c1 =  $len - 1 )
	    #foreach($1 in  $a) 
  			#if(  $c <  $len )   ${allColumns[$c].actualColumnName}, #set($c = $c + 1)   #end
	    #end
    </sql>
    
     
</mapper>